#!/bin/sh

kbmap () {
    setxkbmap us,$1 ,winkeys ctrl:swapcaps grp_led:caps grp:toggle
    exit
}

case $1 in
  d) cmd='xterm -e dict';;
  m) cmd='xterm -e ncmpc';;
  r|ru) kbmap ru;;
  u|ua) kbmap ua;;
  *) exit 1;;
esac

shift
$cmd "$@"
